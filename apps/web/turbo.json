{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": [],
      "passThroughEnv": ["TZ", "NOW_*", "PWD", "SHLVL", "LANG", "FAKEROOTKEY", "ENABLE_VC_BUILD", "ECS_*", "AWS_*", "DD_*"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
